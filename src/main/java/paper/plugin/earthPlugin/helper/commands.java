package paper.plugin.earthPlugin.helper;

import org.bukkit.plugin.java.JavaPlugin;
import org.bukkit.command.Command;
import org.bukkit.command.CommandExecutor;
import org.bukkit.command.CommandSender;

public class commands extends JavaPlugin {
    // command helpers
    // Current idea is to pass the function in here and then when someone executes the command it runs that function

    public static void createCommand(Runnable func, String command) {
        // ..
    }
}

// hello bro i will molest you